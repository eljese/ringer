{
  "name": "pr-train-delivery",
  "version": 1,
  "purpose": "Single-PR implementation under the authoritative PR-train supervisor",
  "entrypoint": "tools/ringer_supervisor_pr_train.py",
  "promotion_owner": "codex-pr-train-controller",
  "implementation_owner": "ringer",
  "review_owner": "agy",
  "invariants": [
    "one implementation task",
    "one supervisor invocation",
    "worker never stages commits pushes or creates pull requests",
    "allowed_changed_paths is non-empty and exact",
    "objective_checks is non-empty and uses argv arrays",
    "canonical supervisor-outcome.json is the only PASS authority",
    "candidate source changes remain uncommitted for controller promotion"
  ]
}
